# **7/12(日)**
## 【学習時間】
7.75h(累計:1392h/1,000h)  

## 【学習内容】
- アプリ開発
___
## 卒制概要
日本酒の記録アプリ。
## 【やったこと】
- RSpec実装
  - FormObjectのモデルスペック
  - Serviceの単体テスト

【学んだこと】
___
## concern

**`ActiveSupport::Concern`**（Concern）は、複数のクラスで共通利用する処理をモジュール化し、再利用しやすくする仕組みである。

Railsでは慣習として、

- app/controllers/concerns
- app/models/concerns

に配置することが多い。

Concernは通常のModuleを拡張したもので、

- included do
- class_methods do

を利用して、include先へクラスメソッドや設定を追加できる。

## 類似

### Helper

Viewで利用する補助メソッドをまとめる仕組み。

例)

- 日付のフォーマット
- 金額の表示形式

### Decorator

ModelのデータをView向けに整形・装飾するオブジェクト。

Viewに表示ロジックを書きすぎないために利用する。

例)

- ユーザーのフルネーム表示
- 投稿日時の表示形式

## 参考

- Railsガイド - concernを使う
- https://qiita.com/ytaros/items/f62bc06f3118a9dc149f
- https://zenn.dev/moneyforward/articles/5fc1a0e9e3f10f
___
## 正規表現

### 正規表現リテラル

Rubyでは、`/.../` で正規表現（Regexp）を表す。

```ruby
/abc/

# ↓ と同じ
Regexp.new("abc")
```

`"abc"` が文字列リテラルであるのに対し、

```ruby
/abc/
```

は **正規表現リテラル** と呼ばれる。

#### 正規表現リテラルの構成

正規表現リテラルの中では、様々な構文を組み合わせて検索条件を表現する。

```
正規表現リテラル (/.../)
├── 文字をそのまま書く
│   └── /abc/
│
├── 文字クラス
│   └── /[abc]/
│
├── POSIX文字クラス
│   └── /[[:space:]]+/
│
├── 文字種メタ文字
│   └── /\s+/
│
├── 量指定子
│   └── + * ? {n,m}
│
├── グループ化
│   └── (abc)
│
└── アンカー
    └── ^ $
```

### 文字クラス

```ruby
/[abc]/
```

文字クラスは、指定した文字のどれか1文字にマッチする。

例

```ruby
/[abc]/
```

↓

```
a または b または c
```

### POSIX文字クラス

```ruby
/[[:space:]]+/
```

の `[:space:]` は **POSIX文字クラス** と呼ばれる。

`[[:space:]]` は空白文字（半角スペース、タブ、改行など）にマッチする。

#### `[[:space:]]` の構造

```
[
  [:space:]
]
```

- 外側の `[ ]`
    - 文字クラス
- 内側の `[:space:]`
    - POSIX文字クラス名

#### 主なPOSIX文字クラス

```ruby
[[:space:]]  空白文字
[[:digit:]]  数字
[[:alpha:]]  アルファベット
[[:alnum:]]  英数字
```

### 文字種メタ文字

```ruby
/\s+/
```

の `\s` は **文字種メタ文字** と呼ばれる。

#### 主な文字種メタ文字

```ruby
\s   空白文字
\d   数字
\w   英数字と _
```

___

## normalizes

### 概要

Active Recordの属性値の正規化（normalization）を宣言するメソッド。

`validates` メソッドのようにクラス定義の中で宣言的に使用できる。

例)

```ruby
class User < ApplicationRecord
  normalizes :email, with: ->(email) { email.downcase }
end
```

### 適用タイミング

- 属性への代入や更新時に適用され、**正規化済みの値が DB に保存される**
- **finder メソッドのキーワード引数**（`find_by` / `find_or_create_by` / `find_or_initialize_by` 等の検索条件）にも適用される → 重複防止のための「保存値と検索キーの一致」が自動で担保される

### 備考

- Rails 7.1で導入されたメソッドである。
- デフォルトでは `nil` 値はスキップされる(正規化が走らない)
- `normalizes` は ActiveRecord 限定の機能
    - `ActiveModel::Model` ベースのフォームオブジェクトでは使えない。
    - FormObject 側で同等のことをしたい場合は、`ActiveModel::Validations::Callbacks` を include して `before_validation` で自前正規化する。

### 参考

https://techracho.bpsinc.jp/hachi8833/2023_06_06/130315

___
## SimpleCovの設定

- `Gemfile`にSimpleCovを追加し、`bundle install`を実行。
- テストヘルパーの**最上部**（`spec/rails_helper.rb`等）にSimpleCovを読み込み、起動する。
    
    ```ruby
    require 'simplecov'
    SimpleCov.start
    ```
    
    - SimpleCovはRails読み込み前に起動する必要がある。
        - Rails環境の読み込み時には、モデルクラス定義やinitializerなど、一度だけ実行されるコードが存在する。SimpleCov起動前にRailsを読み込むと、Railsロード時に実行されたコードが正しくカバレッジ計測されない。
        - 例) `sake.rb`
            
            ```ruby
            class Sake < ApplicationRecord
              PRODUCT_NAME_MAX_LENGTH = 255           # ← require 時に実行される
              normalizes_text :product_name           # ← require 時に実行される
              validates :product_name, presence: true # ← require 時に実行される
              scope :search_by_product_name, ...      # ← require 時に実行される
            end
            ```
            
            このコードは `Sake` クラスがrequireされた時点でRubyにより実行される。そのためSimpleCovはRailsロードより前に開始する。
            

### 補足

#### spec/rails_helper.rbの読み込みの流れ

```ruby
require "simplecov"                          # ① Rails はまだロードされていない
SimpleCov.start "rails" do ... end           #    （だからここでカバレッジ計測を開始）

require "spec_helper"                         # ② RSpec の基本設定
ENV["RAILS_ENV"] ||= "test"                  #    環境を test に固定
require_relative "../config/environment"     # ★③ ここで Rails アプリ全体が読み込まれる
abort(...) if Rails.env.production?           #    ↑以降は Rails / app のクラスが使える
require "rspec/rails"                         # ④ Rails 連携の RSpec 機能を読み込む
# Add additional requires below this line.    
require "webmock/rspec"                       # ⑤ Rails ロード後に追加の require

```

- 冒頭でSimpleCovを読み込まないと、SimpleCovの計測開始前に実行されたコードはカバレッジとして正しく記録されない

### 参考

- https://github.com/simplecov-ruby/simplecov/tree/v0.22.0

___

## Shoulda Matchers概要

Shoulda Matchersとは、Railsのモデルや関連などに対するテストを、RSpecで簡潔に記述するためのマッチャーライブラリ。

通常RSpecで複数行必要な検証を、1行の期待値として書けるようになる。

### マッチャー例

- マッチャー一覧
    - https://github.com/thoughtbot/shoulda-matchers#matchers
- ActiveModelマッチャー
    - `validate_presence_of`
        - 対象属性にpresence validationが設定されていることを検証する。
        - nilや空文字など、存在しない値を無効として扱うことを確認する。
    - `validate_length_of`
        - length validationが設定されていることを検証する。
        - minimum / maximumなどの文字数制限を確認できる。
        - https://matchers.shoulda.io/docs/v8.0.1/Shoulda/Matchers/ActiveModel.html#validate_length_of-instance_method
- ActiveRecord マッチャー
    - `validate_uniqueness_of`
        - uniqueness validationが設定されていることを検証する。
        - 同じ値を持つレコードが存在する場合、validationエラーになることを確認する。

### 注意

Shoulda Matchersはvalidationの設定を確認するもので、DB制約(unique indexなど)を保証するものではない。

### 参照

- https://github.com/thoughtbot/shoulda-matchers
- https://matchers.shoulda.io/docs/v8.0.1/
- https://www.miracleave.co.jp/contents/1487/rspec-shoulda-matchers/#index_id1

___
## RSpceのchangeマッチャの使い方

### 使い方

- 基本的な使い方：`expect { 何かの処理 }.to change { 監視したい値 }`
- 誤り：`expect(何かの処理).to change { 監視したい値 }`
    - `expect(何かの処理)` の時点で処理が実行されてしまう。
    - `change` マッチャは、ブロック内の処理の実行前後で値を比較するため、`expect()` に処理を書いてしまうと変更前の値を取得できず、前後の比較ができない。

### 補足

`change` マッチャは、次のような流れで動作する。

1. 監視対象の現在の値を取得する
2. `expect { ... }` のブロック内の処理を実行する
3. 監視対象の値を再度取得する
4. 前後の差分を比較する

そのため、変更を検証したい処理は必ずブロック (`{ ... }`) に書く。

### 参考

https://zenn.dev/oz006/articles/bb4b270befd333

___

## RSpec_モック・スタブの作成

| 書き方 | 作成 | 振る舞いを差し替える | 呼ばれたことを検証する |
| --- | --- | --- | --- |
| `allow(obj).to receive(:foo)` | スタブ | ○ | × |
| `expect(obj).to receive(:foo)` | モック（Message Expectation） | ○ | ○ |

### 補足

- `allow`
    
    ```
    allow(user).toreceive(:name).and_return("Alice")
    ```
    
    - `name` が呼ばれたら `"Alice"` を返す
    - 呼ばれなくてもテストは成功
- `expect`
    
    ```
    expect(user).toreceive(:name).and_return("Alice")
    ```
    
    - `name` が呼ばれたら `"Alice"` を返す
    - **必ず `name` が呼ばれることを検証する**
    - 呼ばれなければテスト失敗

___
## `send` メソッド

`send` は **Ruby標準のメソッド**（`Object`クラスのメソッド）で、メソッド名を動的に指定して呼び出すために使用する。

### 基本構文

```ruby
object.send(:method_name, 引数...)
```

### 例

```ruby
method_name = :upcase

"ruby".send(method_name)
# => "RUBY"
```

### private メソッドの呼び出し

`send` は 可視性（public/private/protected）を無視してメソッドを呼び出すため、**private メソッドも呼び出せる**。

```ruby
client.send(:fetch_data, :areas)
```

RSpecでは、**private メソッドをテストする際によく使用される**。

### `public_send` との違い

| メソッド | private を呼べる？ | 用途 |
| --- | --- | --- |
| `send` | ✅ 呼べる | メソッド名を動的に指定して呼び出す（可視性を無視） |
| `public_send` | ❌ 呼べない | メソッド名を動的に指定して呼び出す（publicのみ） |

___
## `RuntimeError`

`RuntimeError` は **Ruby標準の例外クラス**で、実行時に発生する一般的なエラーを表す。

`raise` に例外クラスを指定しない場合は、デフォルトで `RuntimeError` が発生する。

```ruby
raise "エラー"
# ↓ と同じ
raise RuntimeError, "エラー"
```

### Rubyのエラー体系

```markdown
Exception
├── StandardError
│   ├── RuntimeError
│   ├── ArgumentError
│   ├── NoMethodError
│   ├── TypeError
│   ├── ZeroDivisionError
│   └── ...
├── ScriptError
├── SystemExit
├── NoMemoryError
└── ...
```

### Javaとの対応イメージ

| Java | Ruby |
| --- | --- |
| `Throwable` | `Exception` |
| `Exception` | `StandardError` に近い |
| `RuntimeException` | `RuntimeError` |
    
___

## `include` マッチャ

`include` は、**指定した要素が含まれていること**を確認するRSpecのマッチャ。

- Hash：指定したキー・値の組が含まれていることを確認
- Array：指定した要素が含まれていることを確認
- String：指定した文字列が含まれていることを確認

```ruby
expect(hash).to include("id" => 1, "name" => "岩手")
```

→ Hash全体ではなく、指定したキー・値が含まれていればテストは成功する。

### `eq` との違い

| マッチャ | 比較方法 |
| --- | --- |
| `include` | 指定した要素（キー・値など）が含まれていればOK |
| `eq` | オブジェクト全体が完全一致している必要がある |

```ruby
hash = {
  "id" => 1,
  "name" => "岩手",
  "code" => "03"
}

expect(hash).to include("id" => 1, "name" => "岩手") # 成功
expect(hash).to eq({ "id" => 1, "name" => "岩手" }) # 失敗
```

`include` は必要な要素だけを確認したい場合、`eq` はオブジェクト全体を比較したい場合に使用する。

___

## RSpec Hooks (フック)

`before` や `after` は、**テストの実行前後に共通処理を実行するためのRSpecのフック（ライフサイクルフック）**。

### 主なフック

| フック | 実行タイミング |
| --- | --- |
| `before`（=`before(:each)`） | 各 `it` の実行前 |
| `after`（=`after(:each)`） | 各 `it` の実行後 |
| `before(:context)`（=`before(:all)`） | `describe` / `context` ごとに1回、最初に実行 |
| `after(:context)`（=`after(:all)`） | `describe` / `context` ごとに1回、最後に実行 |

### 例

```ruby
before do
  create(:area)
end
```

→ 各 `it` の実行前に `create(:area)` が実行される。

### JUnit4との対応

| JUnit4 | RSpec |
| --- | --- |
| `@Before` | `before` |
| `@After` | `after` |
| `@BeforeClass` | `before(:context)` |
| `@AfterClass` | `after(:context)` |

> Rails + RSpecでは、`before`（=`before(:each)`）を使用することが多い。
>

___
## `reload`

`reload` は **Active Record のメソッド**で、**データベースから最新の状態を再読み込みする**。

主に、他の処理でレコードが更新された後に、最新の値を取得したい場合に使用する。

```ruby
user.reload
```

`reload` はオブジェクト自身を返すため、メソッドチェーンで書ける。

```ruby
expect(user.reload.name).to eq("Alice")
```

> `update_all` などでDBを直接更新した場合、メモリ上のオブジェクトは更新されないため、`reload` が必要になることがある。
>


# **7/30(木)**
## 【学習時間】
2.1h(累計:1,451h/1,000h)  

## 【学習内容】
- 卒制

___

## 卒制概要
日本酒の記録アプリ。
## 【やったこと】
画像投稿機能作成(途中)
___

## FactoryBot 連番とテスト順序依存によるユニーク制約違反

### 発生したエラー

RSpec を `--seed 14481` で実行したときだけ、次のテストが失敗した
（seed によって成否が変わる ＝ テスト実行順に依存した失敗）。

```
PG::UniqueViolation: duplicate key value violates unique constraint "index_areas_on_sakenowa_id"
DETAIL: Key (sakenowa_id)=(1) already exists.
```

### 発生した経緯

テスト内で次の順に実行していた。

```ruby
create(:area, sakenowa_id: 1)          # ① sakenowa_id=1 のエリアを明示的に作成
create(:brewery, sakenowa_id: 101)     # ② association により Area を自動生成
```

- ② の `create(:brewery)` は factory の `association :area` で Area を**自動生成**する
- その Area の `sakenowa_id` は factory の連番で採番される（`sequence(:sakenowa_id) { |n| n }` → 1, 2, 3…）
- 連番がたまたま **1** を返すと、① の `sakenowa_id: 1` と衝突してユニーク制約違反になる
- 連番が 1 を返すかどうかは「そのテストが実行される順番（seed）」で変わる → 順序依存の失敗

### 補足: 連番とロールバックの関係（重要）

DB のレコードと FactoryBot の連番は、**別々の仕組み**で動いている。

| 対象 | 仕組み | テスト間で |
| --- | --- | --- |
| DB のレコード | `use_transactional_fixtures = true` により各テストをトランザクション実行し、終了時にロールバック | **残らない**（毎回まっさら） |
| FactoryBot の連番 | Ruby プロセスのメモリ上のカウンタ。DB のトランザクションとは無関係 | **残る（増え続ける）** |
- DB はテストごとにロールバックされ、レコードは残らない
- しかし連番はメモリ上の値なので巻き戻らず、通し番号で増え続ける
- この「**DB はクリーンなのに連番だけ進んでいる**」ズレが、
同一テスト内で明示値（1）と自動採番値（1）を衝突させた

### 対応方針

連番が返す**具体的な値に依存しない**。
固定値（`sakenowa_id: 1`）を明示するテストでは、関連レコードも自分で明示的に紐付け、
factory の自動生成（＝連番採番）を混ぜない。

### 対応内容

明示的に作った Area を Brewery にも `area: area` で渡し、association による Area 自動生成を止めた。

```ruby
# 変更前
create(:area, sakenowa_id: 1)
kept = create(:brewery, sakenowa_id: 101, is_deleted: false)

# 変更後
area = create(:area, sakenowa_id: 1)
kept = create(:brewery, sakenowa_id: 101, area: area, is_deleted: false)
```

- 複数テストで共通のエリアが必要な箇所は、`before { create(...) }` を
`let!(:area) { create(...) }` に変更し、各 Brewery に `area: area` を渡した
- `-seed 14481`  や他のseed値でテストを再実行し全てパスすることを確認した。

___
## RSpecの`subject`まとめ

### `subject`とは

- テスト対象となるオブジェクトを定義するもの。
- `subject { build(:user) }` と書くと、`subject`で`build(:user)`を参照できる。
- `is_expected`は`expect(subject)`の省略形。

```ruby
subject { build(:user) }

it { is_expected.to be_valid }
# ↓ 同じ意味
it { expect(subject).to be_valid }
```

### `subject`を定義しない場合

`subject`を定義しない場合、RSpecが自動で

```ruby
subject { described_class.new }
```

を用意する。

モデルスペックでは

```ruby
RSpec.describe User, type: :model
```

の場合、

```ruby
subject { User.new }
```

として扱われる。

### Shoulda Matchersとの関係

- `have_many`や`validate_presence_of`などのShoulda Matchersは`subject`を対象に実行される。
- アソシエーションのテストはモデル定義（`has_many`など）を検査するため、`User.new`で十分。
- `be_valid`のように実際のバリデーションを行うテストでは、有効な属性を持つ`build(:user)`を`subject`にすることが多い。

___

## Rails アップデート手順

### 1. 現状の確認

- 現バージョンで全テストがパスすることを確認する（テストが薄いなら先に補強）
- deprecation 警告を確認し、**あれば現行バージョン上で解消しておく**
    - テストを流す（test環境は警告が stderr に出る）
    - 過去のログを確認（ `grep "DEPRECATION WARNING" log/development.log | sort -u` ）
    - 実際に画面を触って確認（テストが通らないパスを補完）

### 2. 事前確認

- 新しい Rails が要求する Ruby バージョンと、現在の Ruby を確認する
- 使用 gem が新しい Rails に対応しているか確認（アプリ系だけでなくテスト系 gem も）
- リリースノート・アップグレードガイドの該当バージョンの章に目を通す

### 3. Update 手順

※ 各ステップの後に必ずテストを流し、通ってからコミットして次へ進む

1. **現行バージョンの最新パッチ**まで上げる（例: 7.2.3 → 7.2.x 最新）
2. **次のマイナーバージョン**へ上げる（例: 7.2 → 8.0。一気に飛ばさない）
    - Gemfile のバージョン指定を変更し `bundle update rails`
3. `bin/rails app:update` で設定ファイルを更新
    - 全コミット済みの状態で実行 → `git diff` で差分を確認し、消された自前設定を復元する
4. `new_framework_defaults_X_Y.rb` を**1項目ずつ**有効化して動作確認
    - 全項目有効化できたら `config.load_defaults` を新バージョンに引き上げ、
    defaults ファイルは削除する

### 4. Update 後の検証

- 全テストが緑・Lint / セキュリティスキャンがクリーン（rubocop / brakeman）
- 主要フローを手動確認
- デプロイして本番動作確認（問題時はマージコミットを revert してロールバック）

___

## RailsのSelf参照アソシエーション

### Self参照アソシエーションとは

同じテーブル同士を関連付けるアソシエーション。

通常は

```ruby
belongs_to :area
```

のように別テーブルを参照するが、今回は `Brewery` が `Brewery` を参照する。

```
Brewery
   │ merged_into_id
   ▼
Brewery
```

---

### belongs_to :merged_into

```ruby
belongs_to :merged_into,
           class_name: "Brewery",
           optional: true
```

#### 役割

「自分が統合された先の蔵元」を取得する。

```ruby
brewery.merged_into
```

#### オプション

- `class_name: "Brewery"`
    - 参照先モデルが `Brewery` であることをRailsに伝える。
- `optional: true`
    - `merged_into_id` が `NULL`（未統合）でも保存可能。

---

### has_many :merged_from

```ruby
has_many :merged_from,
         class_name: "Brewery",
         foreign_key: :merged_into_id,
         inverse_of: :merged_into,
         dependent: :restrict_with_exception
```

#### 役割

「自分を統合先としている蔵元一覧」を取得する。

```ruby
brewery.merged_from
```

#### オプション

- `class_name: "Brewery"`
    - 関連先モデルは `Brewery`
- `foreign_key: :merged_into_id`
    - 関連付けに `merged_into_id` カラムを使用する
- `inverse_of: :merged_into`
    - `merged_into` と逆方向の関連であることをRailsに伝え、オブジェクトの整合性やキャッシュを最適化する
- `dependent: :restrict_with_exception`
    - 統合元が存在する場合は削除を禁止し、例外を発生させる

---

### イメージ図

```markdown
Brewery テーブル 例

+----+------------+----------------+
| id | name       | merged_into_id |
+----+------------+----------------+
| 1  | 獺祭株式会社 | NULL           |
| 2  | 旭酒造      | 1              |
| 3  | 旭酒造(旧)   | 1              |
+----+------------+----------------+
```

#### `merged_into`（統合先を取得）

```ruby
brewery = Brewery.find(2)

brewery.merged_into
```

↓

```markdown
      merged_into
+-------------------+
| Brewery(id=2)     |
| 旭酒造            |
+-------------------+
          │
          │
          ▼
+-------------------+
| Brewery(id=1)     |
| 獺祭株式会社      |
+-------------------+
```

#### `merged_from`（統合元を取得）

```ruby
brewery = Brewery.find(1)

brewery.merged_from
```

↓

```markdown
               merged_from

                Brewery(id=1)
               獺祭株式会社
                     ▲
         ┌───────────┴───────────┐
         │                       │
         │                       │
+-------------------+   +-------------------+
| Brewery(id=2)     |   | Brewery(id=3)     |
| 旭酒造            |   | 旭酒造(旧)         |
+-------------------+   +-------------------+
```

---

### メリット

- SQLを書かずに関連データへアクセスできる
- `brewery.merged_into`
- `brewery.merged_from`
- `includes(:merged_into)` などRails標準の機能を活用できる
- 可読性・保守性が高い設計になる

___
## RSpec `aggregate_failures`

### 概要

`aggregate_failures` は、**複数の `expect` を最後まで実行し、失敗をまとめて報告する** RSpec の機能。

### 通常のテストとの違い

#### 通常

- 最初の `expect` が失敗すると、その時点でテスト終了
- 後続の `expect` は実行されない

#### `aggregate_failures do ～ end` で囲う

- 途中で失敗しても最後まで `expect` を実行
- すべての失敗内容をまとめて表示

### 使用例

```ruby
aggregate_failures do
  expect(user.name).to eq("Shuhei")
  expect(user.age).to eq(30)
end
```

### 使用する場面

**1つの仕様を複数の観点から検証したい場合**に適している。

例：

- 「`name` と `area_id` の両方が一致しない限り統合しない」
    - 同名・別エリア
    - 同エリア・別名

このように1つの仕様を複数のケースで確認する場合に有効。

### メリット

- どこまで壊れているか一度に確認できる
- デバッグしやすい
- 関連する期待値を1つのテストにまとめられる

___

## style属性

- HTML要素に直接CSSを書くための属性
- 要素ごとに動的なCSSを指定したいときに使う

```html
<span style="left: 82%; top: 63%;"></span>
```

### 今回の用途

```
<span
  class="absolute"
  style="<%= quadrant_plot_point_style(taste, aroma) %>">
</span>
```

- `absolute`：親要素内で自由な位置に配置できるようにする
- `style`：`left`・`top` を動的に指定し、マップ上の座標へ点を配置する

※ `class` は固定のスタイル、`style` は要素ごとに変わるスタイル（座標など）の指定に向いている。

___

## ActiveStorageとcludinaryの使い方

### 参考

- cloudinary Gem
    - https://cloudinary.com/documentation/rails_activestorage
    - https://github.com/cloudinary/cloudinary_gem
- https://qiita.com/nagumo01/items/76b97a98e83604150496

___

## filter_map

`filter_map`は、**`map`と`compact`（`nil`の削除）を一度に行うRubyのメソッド**。

- 各要素を変換（`map`）
- `nil`になった要素を自動で除外（`compact`）

### mapとの違い

```ruby
[1, 2, 3, 4].map do |n|
  n if n.even?
end
#=> [nil, 2, nil, 4]
```

### filter_map

```ruby
[1, 2, 3, 4].filter_map do |n|
  n if n.even?
end
#=> [2, 4]
```

### Railsでの使用例

```ruby
IMAGE_ATTACHMENT_NAMES.filter_map do |attachment_name|
  attachment = public_send(attachment_name)
  [attachment_name, attachment] if attachment.attached?
end
```

添付されている画像だけを取得し、

```ruby
[
  [:front_label_image, attachment],
  [:sub_image1, attachment]
]
```

のような「配列の配列」を返す。

### ポイント

- `map + compact`をまとめて書ける
- `nil`を返した要素は自動で除外される
- 「条件に合う要素だけを変換して取得したい」場合によく使われる

___

## public_send

`public_send`は、**引数で指定した名前のpublicメソッドを呼び出すメソッド**。

引数には、**メソッド名を表すシンボルまたは文字列**を渡す。

### 基本例

```ruby
class User
  def name
    "Taro"
  end
end

user = User.new

method_name = :name
user.public_send(method_name)
#=> "Taro"
```

上記は次と同じ意味。

```ruby
user.name
```

### Railsでの使用例

```ruby
IMAGE_ATTACHMENT_NAMES = [
  :front_label_image,
  :back_label_image,
  :sub_image1,
  :sub_image2
]

IMAGE_ATTACHMENT_NAMES.each do |attachment_name|
  attachment = public_send(attachment_name)
end
```

ループごとに

```ruby
public_send(:front_label_image)
public_send(:back_label_image)
public_send(:sub_image1)
public_send(:sub_image2)
```

が実行され、それぞれ

```ruby
front_label_image
back_label_image
sub_image1
sub_image2
```

メソッドを呼び出している。

※attachmentには次のように格納される。

```ruby
attachment = self.front_label_image
```

### Active Storageとの関係

```ruby
has_one_attached :front_label_image
```

と書くと、Railsが内部で

```ruby
front_label_image
```

というpublicメソッドを自動生成する。

そのため、

```ruby
public_send(:front_label_image)
```

で添付ファイルオブジェクトを取得できる。

### sendとの違い

- `send`：public・protected・privateメソッドを呼び出せる
- `public_send`：**publicメソッドのみ**呼び出せる（より安全）

### ポイント

- メソッド名を変数で扱える
- 同じ処理を複数のメソッドに対してループで実行できる
- Railsでは動的にメソッドを呼び出したい場合によく使われる


___

## Rubyの`begin-rescue-end`と`ensure`

### `end`の後の処理

`begin-rescue-end`（または`transaction do ... rescue ... end`）の**後に書いたコード**は、例外が適切に処理されていれば成功・失敗に関係なく実行される。

```ruby
saved = false

SakeLog.transaction do
  ...
rescue ActiveRecord::RecordInvalid
  raise ActiveRecord::Rollback
end

purge_removed_images if saved
```

上記では、

- 保存成功 → `purge_removed_images`まで実行
- 保存失敗（ロールバック）→ `purge_removed_images`まで実行（ただし`saved == false`なので処理は行われない）

---

### `ensure`との違い

`ensure`は**例外が発生しても必ず実行される**。

```ruby
begin
  ...
ensure
  cleanup
end
```

Javaの`finally`に最も近い構文。

---

### `end`後の処理と`ensure`の違い

#### `end`後の処理

- 例外が処理されていれば実行される
- 未処理の例外が外へ伝播すると実行されない

#### `ensure`

- 未処理の例外があっても必ず実行される

---

### 今回の実装で`ensure`を使わない理由

今回の`save`メソッドでは、

- `ActiveRecord::RecordInvalid`は`rescue`で処理される
- `ActiveRecord::Rollback`は`transaction`が吸収する

ため、`end`後の処理で十分。

また、`purge_removed_images`は**トランザクションが正常終了（コミット）した後だけ実行したい処理**なので、

```ruby
purge_removed_images if saved
```

のように`transaction`の外へ書く方が意図が分かりやすい。

`ensure`はファイルのクローズやMutexの解放など、**例外の有無に関係なく必ず実行したい後始末**に使うことが多い。

___

## Marcelとは

- Marcelは、Ruby/RailsでファイルのMIME Type（ファイル形式）を判定するライブラリ。
- RailsのActive Storageが内部で利用しているため、通常は別途gemを追加する必要はない。

### Marcel::MimeType.for

```ruby
Marcel::MimeType.for(file)
```

- ファイルの**中身（マジックバイト）**から実際のMIME Typeを判定する。
- `blob.content_type` のようにブラウザが申告した値ではなく、実際のファイル形式を確認できる。
- 拡張子だけを変更した不正なファイル（例: `.txt` を `.jpg` にリネームしたもの）も正しく判定できる。

例:

- JPEG → `image/jpeg`
- PNG → `image/png`
- PDF → `application/pdf`
- テキスト → `text/plain`

___

## `**`（ダブルスプラット演算子）

`**` は、**キーワード引数（Hash）を受け取ったり、展開したりするRubyの演算子**。

### 受け取る場合

```ruby
def image_tag(width:, **options)
end
```

```ruby
image_tag(
  width: 400,
  class: "rounded",
  alt: "ラベル画像"
)
```

受け取る値

```ruby
width
# => 400

options
# => { class: "rounded", alt: "ラベル画像" }
```

### 展開する場合

```ruby
cl_image_tag(
  attachment.blob.key,
  width: width,
  **options
)
```

`options` が

```ruby
{ class: "rounded", alt: "ラベル画像" }
```

なら、

```ruby
cl_image_tag(
  attachment.blob.key,
  width: width,
  class: "rounded",
  alt: "ラベル画像"
)
```

と同じ意味になる。

### と `*` の違い

| 記法 | 扱うもの | 例 |
| --- | --- | --- |
| `*args` | 配列（Array） | `foo(1, 2, 3)` |
| `**options` | キーワード引数（Hash） | `foo(name: "GinLog")` |

### Railsでよく使われる場面

HelperやView Helperで、`class`、`id`、`data`、`loading`などのHTML属性を、そのまま`image_tag`や`link_to`へ渡すためによく使用される。

```ruby
def sake_log_image_tag(attachment, width:, **options)
  image_tag(attachment, **options)
end
```

これにより、引数を増やさず柔軟にHTML属性を渡せる。

___

## `cl_image_tag`

`cl_image_tag`は、**Cloudinary gemが提供する画像表示用のヘルパーメソッド**。

Rails標準の`image_tag`に相当し、Cloudinary上の画像を変換オプション付きで表示できる。

### 基本形

```ruby
cl_image_tag(public_id, options)
```

- 第1引数：Cloudinaryの`public_id`（GinLogでは`attachment.blob.key`）
- 第2引数以降：画像変換やHTML属性のオプション

### 今回のコードのオプション

| オプション | 説明 |
| --- | --- |
| `width: 400` | 幅400pxで配信する |
| `crop: :limit` | 元画像より大きく拡大しない |
| `fetch_format: :auto` | WebP・AVIFなど最適な画像形式を自動選択 |
| `quality: :auto` | 見た目を保ちながら自動圧縮 |
| `**options` | `class`や`alt`などのHTML属性をそのまま渡す |

### 例

```ruby
cl_image_tag(
  attachment.blob.key,
  width: 400,
  crop: :limit,
  fetch_format: :auto,
  quality: :auto,
  class: "rounded-lg"
)
```

#### メモ

Cloudinaryは**画像を保存するだけでなく、配信時にリサイズ・形式変換・圧縮を行う**ため、高画質を保ちながら通信量を削減できる。

#### 参考

- https://cloudinary.com/documentation/rails_image_manipulation

___

## `cloudinary_storage?`

現在のActive Storageの保存先が**Cloudinaryかどうか**を判定するヘルパーメソッドである。

```ruby
def cloudinary_storage?
  defined?(ActiveStorage::Service::CloudinaryService) &&
    ActiveStorage::Blob.service.is_a?(ActiveStorage::Service::CloudinaryService)
end
```

### 処理内容

1. `defined?`
    - `ActiveStorage::Service::CloudinaryService`クラスが**存在するか**を確認する。
    - クラスが存在しない場合は、 `nil`(falsey扱い)
    - 左辺が`false`の場合 `&&`の短絡評価により、右辺 `ActiveStorage::Blob.service.is_a?(ActiveStorage::Service::CloudinaryService)` は実行されず、`NameError (uninitialized constant)` を防げる。
2. `&&`（短絡評価）
    - 左辺が`nil`または`false`なら右辺は評価されないため、安全に判定できる。
3. `△△.is_a?(〇〇)`
    - この△△オブジェクトは○○クラスか?
4. `ActiveStorage::Blob.service`
    - 現在Active Storageが使用している保存サービスを取得する。
5. `is_a?(ActiveStorage::Service::CloudinaryService)`
    - 現在の保存サービスがCloudinaryかどうかを判定する。

___

## `cloudinary_storage?` メソッド(独自メソッド)

現在のActive Storageの保存先が**Cloudinaryかどうか**を判定するヘルパーメソッドである。

```ruby
def cloudinary_storage?
  defined?(ActiveStorage::Service::CloudinaryService) &&
    ActiveStorage::Blob.service.is_a?(ActiveStorage::Service::CloudinaryService)
end
```

### 処理内容

1. `defined?`
    - `ActiveStorage::Service::CloudinaryService`クラスが**存在するか**を確認する。
    - クラスが存在しない場合は、 `nil`(falsey扱い)
    - 左辺が`false`の場合 `&&`の短絡評価により、右辺 `ActiveStorage::Blob.service.is_a?(ActiveStorage::Service::CloudinaryService)` は実行されず、`NameError (uninitialized constant)` を防げる。
2. `&&`（短絡評価）
    - 左辺が`nil`または`false`なら右辺は評価されないため、安全に判定できる。
3. `△△.is_a?(〇〇)`
    - この△△オブジェクトは○○クラスか?
4. `ActiveStorage::Blob.service`
    - 現在Active Storageが使用している保存サービスを取得する。
5. `is_a?(ActiveStorage::Service::CloudinaryService)`
    - 現在の保存サービスがCloudinaryかどうかを判定する。

___

## `cl_image_tag` と `cloudinary_url`

Cloudinaryでは、用途に応じて`cl_image_tag`と`cloudinary_url`を使い分ける。

| メソッド | 戻り値 | 用途 |
| --- | --- | --- |
| `cl_image_tag` | `<img>`タグ | 画像を画面に表示する |
| `cloudinary_url` | 画像URL（String） | 画像URLを取得する（リンク先など） |

### `cl_image_tag`

Cloudinary版の`image_tag`であり、`<img>`タグを生成する。

```ruby
cl_image_tag(attachment.blob.key, width: 400)
```

```html
<img src="<https://res.cloudinary.com/>...">
```

### `cloudinary_url`

変換済み画像のURL文字列を生成する。

```ruby
cloudinary_url(attachment.blob.key, width: 1600)
```

```
<https://res.cloudinary.com/>...
```

### 使い分け

- **画像を表示する** → `cl_image_tag`
- **画像URLだけ欲しい**（リンク先・APIレスポンスなど） → `cloudinary_url`
