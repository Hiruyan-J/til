# 3/3(月)
**【学習時間】**
3.47h(累計:364.6h/1,000h)
  
**【学習内容】**
- Rails基礎
	- Ch20 パスワードリセット機能の実装

**【課題を通して学んだこと】**（知識・タスク分解などの振り返り）  
- Sorcery` のサブモジュールをインストールする際に、公式のサンプルコードをそのまま使用し、余計なサブモジュールもインストールしてしまった
  - 今回は `rails g sorcery:install 任意のサブモジュール --only-submodules` でOK
- Configでローカルホストを設定し、呼び出すとハッシュじゃない形式で呼び出されてしまい、`NoMethodError in PasswordResetsController#create
undefined method merge'` のエラーが発生した
  - 呼び出し時にオブジェクトを `to_h` でハッシュに変換することで解決



# 3/7(金)
**【学習時間】**
2.9h(累計:367.78h/1,000h)
  
**【学習内容】**
- Rails基礎
	- Ch20 パスワードリセット機能の実装　完了 :kanryoudesu: 

**【課題を通して学んだこと】**（知識・タスク分解などの振り返り）  
Lintチェックエラーの対応を実施しました。 :rubocop: 
Gemの公式Git Hubのコードが更新されていない場合、最近のトレンドとは異なってしまっていることや、Rubocopの設定では、自分の環境でははじかれてしまう書き方になっていることを学べた。
例
- Gemの公式では動的ファインダー( `find_by_xxx` )を使用しているが、Rubocopでは `find_by` を推奨してくる
- Gem 公式では、safe nabigation演算子(ぼっち演算子) `&.` を使用していない(ぼっち演算子がruby 2.2.0？で実装されたから？)
____
form_withのsubmitボタンの表示内容について、挙動を調べてみた。備忘録としてメモ📝
  - `<%= f.submit %>` とし、翻訳するテキストを指定しないと以下の動作となる
      - form_withでモデルを指定している場合
          - new.html.erb→ja: helpers: submit: create: のテキストが表示される　例)登録
          - edit.html.erb→ja: helpers: submit: update: のテキストが表示される　例)更新
          - それ以外→ ja: helpers: submit: submit: のテキストが表示される　例)保存
      - form_withでモデルを指定しない場合
          - ja: helpers: submit: submit: のテキストが表示される　例)保存



# 3/9(日)
**【学習時間】**
6.11h(累計:381.9h/1,000h)
  
**【学習内容】**
- チーム開発
  - フラッシュメッセージの実装
- Rails基礎
	- Ch21 管理画面へのログイン機能、管理画面トップページの作成
- GitHub草生やし:seedling:   
**【課題を通して学んだこと】**（知識・タスク分解などの振り返り）  
チーム開発でTailwindCSSを利用して、フラッシュメッセージの実装を行いました。
- カスタムCSS作成方法
	- `app/assets/stylesheets/application.tailwind.css` に、TailWindCSSのカスタムCSS(新しいクラス)を作成
		-  `alert-success` `alert-danger`というクラスのカスタムCSSを作成
		- `@apply` を使用すると、TailwindCSSを複数まとめたカスタムCSSを作成できる
	- <参考>
		- [tailwindにカスタムクラスを追加する](https://zenn.dev/cordelia/scraps/45cbb80daee332)
		- [tailwindcss-Adding custom styles](https://tailwindcss.com/docs/adding-custom-styles#adding-component-classes)
		- [tailwindcss-Functions and directives](https://tailwindcss.com/docs/functions-and-directives#apply)
- 動的に指定したクラス名でもカスタムCSSが適用されるように設定
	- 実施内容    
    `config/tailwind.config.js` の `safelist` オプションに、使用するカスタムCSSクラス（`alert-success` `alert-danger`）を指定した    
    - コード        
        config/tailwind.config.js        
        ```bash
        module.exports = {
          safelist: ['alert-success','alert-danger'],
        }
        ```        
	- 背景    
    カスタムCSSは、`_flash_message` パーシャル内で以下のように動的にクラスを指定している。    
    ```
    <div class="alert-<%= message_type %>">
    ```    
    しかし、Tailwind CSSの仕様により、ビルド時にHTML内で直接確認できるクラスのみが出力されるため、`alert-success` `alert-danger` `alert-notice` `alert-alert` のクラスが適用されなかった。    
    そこで、`config/tailwind.config.js` の `safelist` オプションを利用し、これらのクラスを明示的に指定することで、常に出力するように設定する必要がある。    
	- 参考
    - [Tailwind CSSで、よく使いそうなのにやり方がわからなかったこと](https://qiita.com/webbingstudio@github/items/27cb99334bdfce503282)        
        > Tailwind CSSはバージョン3以降、CSSをビルドしたときに公開ディレクトリのHTMLを解析し、存在しているクラスのみを書き出すようになりました。この場合、例えばJavaScriptやPHPなどで、動的に書き換えられたときのクラスを書き出してくれません。HTMLに存在していないからです。
        > 
    - [Tailwind CSSの嬉しいけど嬉しくない機能 - Just In Time Mode](https://qiita.com/webbingstudio@github/items/27cb99334bdfce503282)
    - [tailwind.config.js のよく使う設定](https://qiita.com/99no_exit/items/809fd7c3cbc2ffcaf7df#safelist)
___
- mainブランチに変更を加えてしまった場合の対処を調べた
  -[ こちらの記事](https://zenn.dev/n_haru2/articles/8e9d6ab74d283b)がいろんなパターンを紹介してくれていてわかりやすかったです！
- 変更したファイル名を列挙	
	```
	git diff --name-only <branch_name,commit_id>
	```
	- 実例    
    `git diff --name-only origin/master`でリモートリポジトリと比較し、差分があるファイル名を列挙してくれる
___
**【その他】**  
チーム開発でTailwindCSSの使い方を色々調べました！
個人の記事を読んで→公式読むという感じで調べました！
公式は英語なので、翻訳機能に頼らないとしんどいですね、、、💦
しかし、翻訳機能も万能ではない、、、



# 3/13(木)
**【学習時間】**
4.4h(累計:387.8h/1,000h)
  
**【学習内容】**
- Rails基礎Ch21

**【課題を通して学んだこと】**（知識・タスク分解などの振り返り） 
- 三項演算子
    
    ```ruby
    条件式 ? 条件が真の場合の値 : 条件が偽の場合の値
    ```
    
    エクセルのif文みたいなもの
    
    条件式の場所は、今回は `admin` 自体がBoolean値だが、 `score => 70` とかでもOK
    
    - 短縮前
        
        ```ruby
        if文の場合
        if 条件式 then
            条件式が真の場合に行う処理
        else
            条件式が偽の場合に行う処理
        end
        ``` 
___
**【その他】**  
ruoteの設定も行ったが、いまいち理解できていないので、明日復習することとする。
