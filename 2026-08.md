# **8/14(金)**
## 【学習時間】
8.33h(累計:1501h/1,000h)  

## 【学習内容】
- アプリ開発
___
## 卒制概要
日本酒の記録アプリ。

## 【やったこと】
- timelineの刷新

___

## Rack と Rack::Test::UploadedFile

### Rackとは

- **Rack** は、Ruby製Webアプリケーション（Rails、Sinatra など）共通のインターフェース（橋渡し役）
- ブラウザからのHTTPリクエストを受け取り、Railsへ渡し、Railsのレスポンスをブラウザへ返す役割を担う

```
ブラウザ
    │ HTTPリクエスト
    ▼
  Rack
    │
    ▼
  Rails
```

- 動作例
    
    例えば以下のフォームの場合を考える。
    
    ```ruby
    <%= f.file_field :front_label_image %>
    ```
    
    ユーザーが
    
    ```
    label.png
    ```
    
    を選択して送信すると Rails のコントローラには
    
    ```ruby
    params[:front_label_image]
    ```
    
    として、単なる文字列ではなく
    
    ```ruby
    ActionDispatch::Http::UploadedFile
    ```
    
    という「アップロードされたファイルオブジェクト」が渡される。
    
    この中には
    
    - ファイル本体
    - ファイル名
    - Content-Type
    - サイズ
    
    などの情報が入っている。
    

### Rack::Test::UploadedFileとは

- `rack-test` が提供する、**アップロードファイルをテスト用に再現するクラス**
- ブラウザから送信されたファイルと同じようなオブジェクトを作成できる
- Active Storage の `attach` に渡すことで、本番とほぼ同じアップロード処理をテストできる
- ファイル名や `Content-Type`（MIMEタイプ）を指定できるため、画像形式やサイズのバリデーションを正確に検証できる

### 使用例

```ruby
def uploaded_file(filename, content_type)
  Rack::Test::UploadedFile.new(
    Rails.root.join("spec/fixtures/files", filename),
    content_type
  )
end

sake_log.front_label_image.attach(
  uploaded_file("label.png", "image/png")
)
```

### ポイント

- `spec/fixtures/files` に置いたテスト用ファイルを読み込む
- `image/png` などの MIME タイプを指定できる
- `attach` 後は Active Storage の Blob・Attachment が作成され、本番とほぼ同じ挙動でテストできる
- Active Storage のモデルスペックでよく利用される定番のクラス

___

## satisfy

- `satisfy` は **RSpecのマッチャ** の1つ
- **任意の条件式を書いて、その結果が `true` ならテスト成功、`false` なら失敗**となる
- 複雑な条件や、既存のマッチャでは表現しにくい条件を検証したいときに便利

### 基本構文

```ruby
expect(値).to satisfy { |変数| 条件 }
```

### 使用例

偶数であることを確認する

```ruby
expect(4).to satisfy { |n| n.even? }   # 成功
expect(5).to satisfy { |n| n.even? }   # 失敗
```

文字列が "Rails" で始まることを確認する

```ruby
expect("Rails 8").to satisfy { |str| str.start_with?("Rails") }
```

### `all` と組み合わせる例

```ruby
expect([2, 4, 6]).to all(satisfy { |n| n.even? })
```

これは内部的に

```ruby
2.even? #=> true
4.even? #=> true
6.even? #=> true
```

を確認しているイメージ。

### 今回のRSpec

```ruby
expect(SakeLog::IMAGE_ATTACHMENT_NAMES)
  .to all(satisfy { |name| sake_log.public_send(name).attached? })
```

- `all` は「配列のすべての要素に対して条件を確認する」マッチャ
- `|name|` には `IMAGE_ATTACHMENT_NAMES` の各要素が順番に入る
    - `:front_label_image`
    - `:back_label_image`
    - `:sub_image1`
    - `:sub_image2`
- `sake_log.public_send(name).attached?` がすべて `true` であることを確認している

イメージすると、次のコードとほぼ同じ意味になる。

```ruby
expect(sake_log.front_label_image.attached?).to eq(true)
expect(sake_log.back_label_image.attached?).to eq(true)
expect(sake_log.sub_image1.attached?).to eq(true)
expect(sake_log.sub_image2.attached?).to eq(true)
```


___

## RSpecのスタブ・モック

### スタブ（Stub）

- **メソッドの戻り値を差し替える**ための仕組み
- テストしたい状況を簡単に再現できる
- 「呼ばれたかどうか」は検証しない

```ruby
allow(user).to receive(:admin?).and_return(true)

user.admin?
#=> true
```

### モック（Mock）

- **メソッドが呼ばれることを検証する**ための仕組み
- 指定したメソッドが呼ばれなければテストは失敗する

```ruby
expect(user).to receive(:admin?)

user.admin?
```

### `allow(...).to receive(...)` の読み方

```ruby
allow(オブジェクト)
  .to receive(:メソッド)
  .and_return(戻り値)
```

日本語にすると

> **「このオブジェクトの、このメソッドが呼ばれたら、この値を返してください。」**
> 
- `allow(...)`：スタブ化したいオブジェクト
- `receive(...)`：スタブ化したいメソッド
- `and_return(...)`：返す値

### GinLogでの使用例

画像サイズのテストでは、本当に10MB超の画像を用意する代わりに、`byte_size` の戻り値だけを差し替えている。

```ruby
allow(sake_log.front_label_image.blob)
  .to receive(:byte_size)
  .and_return(SakeLog::IMAGE_MAX_SIZE + 1)
```

これにより

```ruby
sake_log.front_label_image.blob.byte_size
#=> SakeLog::IMAGE_MAX_SIZE + 1
```

となり、大きな画像を用意せずにサイズ超過のテストができる。

### スタブとモックの違い

| 項目 | スタブ | モック |
| --- | --- | --- |
| 目的 | 戻り値を差し替える | メソッドが呼ばれることを検証する |
| 主な書き方 | `allow(...).to receive(...)` | `expect(...).to receive(...)` |
| 戻り値の変更 | ○ | ○（必要なら `and_return` を付ける） |
| 呼び出しの検証 | × | ○ |


___

## tapメソッド

ブロック内でオブジェクトを加工したあと、元のオブジェクト自身を返すメソッド。

```ruby
"a".tap do |x|
  x.upcase!
  "別の文字"
end
=> "A"
```

### 参考

- https://zenn.dev/ebina_shohei/articles/5b212feaa63413

___

## ActiveJob 学習ノート

> 2026-08-02 / Issue #24（ラベル画像投稿）の CodeRabbit 指摘対応を通じて学んだこと
> 

### 1. ActiveJob は設定なしで動く

`config/application.rb` が `require "rails/all"` なら ActiveJob はロード済み。
アダプタ未設定でも Rails が自動で割り当てる。

```ruby
# activejob/lib/active_job/railtie.rb
options.queue_adapter ||= (Rails.env.test? ? :test : :async)
```

| 環境 | 実効アダプタ |
| --- | --- |
| development / production | `AsyncAdapter` |
| test | `TestAdapter`（自分で設定する必要なし） |

### 2. `:async` アダプタの正体

`Concurrent::ThreadPoolExecutor`＝**メモリ上のキュー**。

```ruby
DEFAULT_EXECUTOR_OPTIONS = {
  max_threads: ENV.fetch("RAILS_MAX_THREADS", 5).to_i,
  max_queue:   0,   # 無制限
}
```

- `max_queue: 0` なので **enqueue は絶対に詰まらない**（リクエストは即座に返る）
- 実行は最大5スレッドの並列。あふれた分はキューで待つだけでエラーにならない
- **プロセスが終わると未実行ジョブは消える**（実測確認済み）

消えるきっかけ（Render 無料プラン）:

| きっかけ | 頻度 |
| --- | --- |
| 15分無通信でスピンダウン | 最も高い |
| PRマージによる自動デプロイ | 開発中は高い |
| クラッシュ / OOM | 稀 |

### 3. `purge` と `purge_later` の違い

どちらも **attachment 行の削除は同期実行**。画面上の挙動は変わらない。

```ruby
def purge_later
  transaction do
    delete                 # ← ①ここは同期（purge と同じ）
    record.touch if record&.persisted?
  end
  blob&.purge_later        # ← ②③をジョブに移すだけ
end
```

|  | `purge` | `purge_later` |
| --- | --- | --- |
| ①attachment 行の削除 | 同期 | 同期 |
| ②blob 行の削除 | 同期 | 非同期 |
| ③外部ストレージから削除 | 同期 | 非同期 |
| リトライ | なし | **なし**（同じ） |
| 通信失敗時の孤児ファイル | 残る | **残る**（同じ） |
| ユーザーに見えるもの | **500 エラー** | 正常レスポンス |

**要点：`purge_later` は信頼性を上げない。例外の発生場所を
リクエストスレッドからジョブスレッドへ移すだけ。**

`ActiveStorage::PurgeJob` がリトライするのは DB のデッドロックのみ。

```ruby
discard_on ActiveRecord::RecordNotFound
retry_on   ActiveRecord::Deadlocked, attempts: 10
# ネットワークエラーは対象外
```

### 4. トランザクション終了 ≠ コネクション返却

DBコネクションは **「単位処理（executor の wrap）」が終わるまで**保持される。
`commit` では返却されない。

```
ジョブ内： blob 行を削除（トランザクション完了）
           → 外部ストレージへ HTTP DELETE（この間もコネクションを握ったまま）
           → ジョブ終了（ここで初めて返却）
```

外部通信が遅いと、その間ずっとコネクションを占有する。

### 5. スレッド／コネクションが足りないときの挙動

**即エラーにはならない。2段階で待つ。**

| 枯渇するもの | 挙動 | エラーになるか |
| --- | --- | --- |
| ジョブスレッド（5） | キューで待つ | **ならない** |
| DBコネクション（5） | **最大 `checkout_timeout` = 5秒**待つ | 5秒以内に空けば正常処理 |

5秒経っても空かなければ `ActiveRecord::ConnectionTimeoutError`。

#### 注意：デフォルト値の食い違い

3か所とも同じ環境変数を読むのに **デフォルト値が違う**。

| 項目 | デフォルト | 定義場所 |
| --- | --- | --- |
| Puma リクエストスレッド | **3** | `config/puma.rb` |
| ジョブスレッド | **5** | AsyncAdapter |
| DBコネクションプール | **5** | `config/database.yml` |

→ コネクションを要求しうるスレッドは 3 + 5 = **8**、プールは **5**。
環境変数を上げても3か所とも上がるので解消しない。対策するなら:

```ruby
# 案1: ジョブスレッドを絞る
config.active_job.queue_adapter = ActiveJob::QueueAdapters::AsyncAdapter.new(max_threads: 2)

# 案2: プールにジョブ分を上乗せする（config/database.yml）
pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 }.to_i + 5 %>
```

### 6. Solid Queue を検討する判断基準

|  | `:async` | Solid Queue（Puma内） | Solid Queue（別ワーカー） |
| --- | --- | --- | --- |
| キューの置き場所 | メモリ | **DB** | **DB** |
| 再起動・スピンダウン | **消失** | 残る（復帰後に処理） | 残る |
| Render の費用 | 無料 | 無料 | **$7/月〜**（Background Worker は有料のみ） |

DBに残るので「**失われる**」が「**遅れる**」に変わるのが本質的な差。

```ruby
# config/puma.rb に記述だけ既に存在する（gem 未導入なので今は有効化しないこと）
plugin :solid_queue if ENV["SOLID_QUEUE_IN_PUMA"]
```

#### 判断基準

- *「そのジョブが消えて困るか」**で決める。
- 消えても困らない（例：ファイル削除 → 孤児ファイルが1つ残るだけ）→ `:async` で十分
- 消えると困る（例：メール送信、決済、データ更新）→ Solid Queue

### まとめ：今回の結論

- 対応した内容 = `purge` → `purge_later` の**1単語**
- 得られたもの = 通信エラー時に 500 を返さなくなること**だけ**
- 得られなかったもの = 削除の確実性（リトライされない・孤児ファイルは残る）
- 現状 `:async` のままで問題ない理由 = 唯一のジョブ `PurgeJob` は消えても実害が小さいから

___


## Active Storage のテーブルと削除タイミング 学習ノート

> 2026-08-02 / Issue #24（ラベル画像投稿）で `purge` → `purge_later` に変更した際に学んだこと
> 

### 1. 3つのテーブルの役割

`bin/rails active_storage:install` で作られる3テーブル。
`sake_logs` 側にカラムは一切増えない。

| テーブル | 役割 | このアプリでの中身 |
| --- | --- | --- |
| `active_storage_blobs` | **ファイル1つ分の情報**。保存先のキー・ファイル名・content_type・バイト数など。実体は Cloudinary にあり、ここはその参照情報 | 画像1枚につき1行 |
| `active_storage_attachments` | **「どのレコードのどのスロットに、どのファイルが付いているか」の中間テーブル**。`record_type` / `record_id` / `name` / `blob_id` を持つ | 1記録につき最大4行 |
| `active_storage_variant_records` | **変換版（バリアント）を生成した記録**。Rails 側でリサイズしたときだけ増える | **常に空**（Cloudinary の URL 変換を使うため） |

`attachments.name` がスロット名を表す（旧設計の `image_category` 相当）。

| name | 意味 |
| --- | --- |
| `front_label_image` | 表ラベル |
| `back_label_image` | 裏ラベル |
| `sub_image1` / `sub_image2` | その他 |

#### 外部キー

```
active_storage_attachments.blob_id → active_storage_blobs.id
```

**この FK が後述の落とし穴の原因になる。**

### 2. `purge_later` はどこで何を消すか

```ruby
class ActiveStorage::Attachment   # ← self は「添付」であって「ファイル」ではない
  def purge_later
    transaction do
      delete                      # ① 自分自身（attachments 行）を削除
      record.touch if record&.persisted?
    end
    blob&.purge_later             # ② blob 側の処理をジョブへ委譲
  end
end
```

②が呼び出すのはこちら。

```ruby
class ActiveStorage::Blob
  def purge
    destroy                            # blobs 行を削除（+ variant_records も連鎖削除）
    delete if previously_persisted?    # Cloudinary のファイルを削除
  rescue ActiveRecord::InvalidForeignKey
  end
end
```

#### 実測した削除タイミング

| タイミング | attachments | blobs | ファイル |
| --- | --- | --- | --- |
| `purge_later` を呼ぶ前 | あり | あり | あり |
| **①の直後**（ジョブ未実行） | **なし** | あり | あり |
| **②のジョブ実行後** | なし | **なし** | **なし** |

|  | 対象 | 実行場所 | 同期/非同期 |
| --- | --- | --- | --- |
| ① | `active_storage_attachments` の行 | リクエストスレッド | **同期** |
| ② | `active_storage_blobs` の行 | ジョブスレッド | 非同期 |
| ② | Cloudinary のファイル | ジョブスレッド | 非同期 |

**要点：①（添付の切り離し）は同期なので、画面上は即座に画像が消える。**
非同期になるのは②だけ。だから `purge` から `purge_later` に変えても
ユーザーから見た挙動は変わらない。

#### variant_records の連鎖

```ruby
# Blob::Representable
has_many :variant_records, dependent: false
before_destroy { variant_records.destroy_all if ActiveStorage.track_variants }
```

`blob.destroy` の `before_destroy` で variant_records も消える。
このアプリでは常に空なので実質何も起きない。

### 3. `purge` との違い

|  | `purge` | `purge_later` |
| --- | --- | --- |
| ①attachments 行 | 同期 | **同期**（同じ） |
| ②blobs 行 | 同期 | 非同期 |
| ②Cloudinary のファイル | 同期 | 非同期 |
| リトライ | なし | **なし**（同じ） |
| 通信失敗時の孤児ファイル | 残る | **残る**（同じ） |
| ユーザーに見えるもの | **500 エラー** | 正常レスポンス |

**`purge_later` は削除の確実性を上げない。**
例外の発生場所をリクエストスレッドからジョブスレッドへ移すだけ。

### 4. 落とし穴：トランザクション内で呼んではいけない

「可読性のため `SakeLog.transaction do` の中に入れたい」は **NG**。

### 理由：ジョブは別コネクションで動くため、①の削除がまだ見えない

```
[リクエストのコネクション]              [ジョブのコネクション（別物）]
SakeLog.transaction do
  purge_later
    ① DELETE attachments ← 未コミット
    ② PurgeJob を enqueue ─────────────→ ジョブ開始
  （待たずに次へ進む）
                                         DELETE blobs
                                           FKチェック：attachments に
                                           まだ行が見える（未コミットだから）
                                           → InvalidForeignKey
                                         rescue で握りつぶす
                                         → 何も消さず「完了」
COMMIT ← 成功（エラーにならない）
end
```

実測結果：

```
外側の SakeLog.transaction = コミット成功（例外なし）
ジョブ側                   = 例外なく完了
添付行は消えたか           = true
blobs 行は残っているか     = true   ← 孤児
ファイルは残っているか     = true   ← 孤児
```

**ユーザーには削除成功と見えるのに、blobs 行とファイルが残る。**
ジョブは「完了」扱いなので再試行もされず、ログにも残らない。

### 誤解しやすい点

| 誤解 | 実際 |
| --- | --- |
| `purge_later` 内の `transaction do` は独立している | **外側に吸収される**（`open_transactions = 1`） |
| 外側のトランザクションがジョブの完了を待つ | **待たない**（enqueue するだけ） |
| 外側のトランザクションがエラーになる | **正常にコミットされる** |
| — | エラーは**ジョブ側**で起き、`rescue` に**握りつぶされる** |

#### 正しい配置

```ruby
    # 画像の削除はトランザクションの外（＝コミット後）で行う。
    # ジョブは別コネクションで動くため、トランザクション内で呼ぶと
    # 「添付行の削除がまだ見えない」状態で実行され、外部キー違反を
    # 握りつぶして何も削除せずに完了してしまう（孤児ファイルが残る）。
    purge_removed_images if saved
```

トランザクション外なら、①がコミット済みになってから②が走るので
FK チェックが通り、blobs 行とファイルが正しく削除される。

#### 例外：どうしても中に入れたい場合

```ruby
# config/application.rb
config.active_job.enqueue_after_transaction_commit = true   # 現在は false
```

enqueue 自体をコミット後まで遅延させれば安全になるが、
**全ジョブの挙動を変えるグローバル設定**なので慎重に。

### まとめ

- `attachments` = 紐付け、`blobs` = ファイル情報、`variant_records` = 変換版の記録
- `purge_later` の「later」になるのは **blobs 行とファイルだけ**。添付の切り離しは同期
- だから画面上の挙動は `purge` と変わらない
- **トランザクションの外で呼ぶこと**。中で呼ぶと FK 違反が握りつぶされ、孤児が静かに残る

___

## `first` と `first(n)` の違い

### `first`

- 先頭の要素を **1つ** 返す
- 戻り値は **要素そのもの**

```ruby
arr = [1, 2, 3]

arr.first
# => 1
```

### `first(n)`

- 先頭から **n個** の要素を返す
- 戻り値は **配列**
- 要素数より大きい `n` を指定しても、取得できる分だけ返す

```ruby
arr = [1, 2, 3]

arr.first(2)
# => [1, 2]

arr.first(5)
# => [1, 2, 3]
```

### ポイント

| 書き方 | 戻り値 |
| --- | --- |
| `first` | 先頭の要素 |
| `first(1)` | 先頭要素を1つ含む配列 |
| `first(n)` | 先頭から n 個の要素を含む配列 |

___

## `min-w-0` と `truncate`

### `min-w-0`

- `min-width: 0;` を指定する Tailwind CSS のクラス。
- Flexbox の子要素を必要に応じて縮められるようにする。
- 長いテキストでレイアウトが崩れるのを防ぐ。
- `truncate` と組み合わせて使われることが多い。

### `truncate`

以下の3つの CSS をまとめた Tailwind CSS のクラス。

```css
overflow: hidden;
white-space: nowrap;
text-overflow: ellipsis;
```

- `overflow: hidden;`
    - 要素からはみ出した部分を隠す。
- `white-space: nowrap;`
    - テキストを改行せず1行で表示する。
- `text-overflow: ellipsis;`
    - はみ出した文字列を `...` で省略表示する。

### よくある組み合わせ

```
<div class="min-w-0">
  <p class="truncate">
    とても長いユーザー名...
  </p>
</div>
```

- `min-w-0` がないと、Flexbox内では `truncate` が正しく動かない場合がある。
- **Flexboxで長い文字列を省略表示する場合は、`min-w-0` と `truncate` をセットで使う**のが定番。

___

## `link_to` と `_path` の引数

### `link_to`

- 基本形

```
<%= link_to URL, オプション do %>
  表示内容
<% end %>
```

- 第1引数：遷移先URL（`_path` や `_url`）
- 第2引数以降：HTML属性や `data` 属性などのオプション

例：

```
<%= link_to sake_log_path(sake_log),
    id: "button-delete",
    data: { turbo_method: :delete } do %>
  削除
<% end %>
```

---

### `_path` の引数

```ruby
sake_log_path(sake_log)
```

- 第1引数：ルートに必要なパラメータ（`id` など）

```
/sake_logs/123
```

---

### クエリパラメータを追加する

```ruby
sake_log_path(sake_log, from: "timeline")
```

生成されるURL

```
/sake_logs/123?from=timeline
```

- `from:` はRailsが用意した特別な引数ではない。
- 開発者が自由に追加できるクエリパラメータ。
- コントローラでは `params[:from]` として受け取れる。

```ruby
if params[:from] == "timeline"
  # タイムラインからの削除
end
```

---

### ポイント

- `_path` にルートで使われないキーワード引数を渡すと、クエリパラメータ（`?key=value`）としてURLに付与される。
- クエリパラメータ名は自由に決められるが、`from` や `page` など意味が分かる名前を付けるのが望ましい。

___

## ブロック引数の `_` と `| |` の書き方

### `_` が付いた変数

```ruby
_attachment_name
```

- 「この変数は使わない」ことを表す慣習。
- Rubyの文法ではなく、可読性のための命名。
- 使わない引数や変数には `_` を付けることが多い。

---

### `| |` はブロック引数

```ruby
collection.each do |item|
```

- `| |` の中に、ブロックへ渡される値を受け取る変数を書く。

---

### `each_with_index`

```ruby
collection.each_with_index do |item, index|
```

- `item`：コレクションの各要素
- `index`：0から始まる添字

---

### `()` は配列を分解して受け取る

```ruby
card_images.each_with_index do |(_attachment_name, attachment), index|
```

`card_images` の各要素は

```ruby
[:front_label_image, attachment]
```

という配列なので、

```ruby
(_attachment_name, attachment)
```

と書くことで

```ruby
_attachment_name = :front_label_image
attachment = attachment
```

のように配列を自動で分解して受け取れる。

`()` がない場合は、配列全体を1つの変数として受け取る。

```ruby
card_images.each_with_index do |item, index|
  attachment_name = item[0]
  attachment      = item[1]
end
```

___

## render と redirect_to の違い

| render | redirect_to |
| --- | --- |
| 現在のリクエスト内でHTMLを返す | ブラウザに別URLへのアクセスを指示する |
| 新しいリクエストは発生しない | 新しいリクエストが発生する |
| URLは変わらない | URLが変わる |
| `flash.now` を使う | `flash` を使う |

### 処理の流れ

#### render

```
ブラウザ
    │
    ▼
Controller
    │
render :index
    │
    ▼
HTMLを返す
```

#### redirect_to

```
ブラウザ
    │
    ▼
Controller
    │
redirect_to ※今回は `status: :see_other` を付けた
    │
    ▼
303 + Location: /sake_logs
    │
    ▼
ブラウザ
    │
GET /sake_logs（新しいリクエスト）
    │
    ▼
Controller#index
    │
    ▼
HTMLを返す
```

### 覚え方

- `render` = **サーバーがその場でHTMLを返す**
- `redirect_to` = **ブラウザに「このURLへ行ってください」と指示する**

___

## Bash：while文・case文・shift

### while文

条件が真（終了ステータスが0）の間、処理を繰り返す。

```bash
while [ 条件 ]; do
  # 繰り返す処理
done
```

例：

```bash
while [ $# -gt 0 ]; do
  # 引数を処理
done
```

- `$#`：渡された引数の個数
- `gt`：greater than（〜より大きい）
- `[ $# -gt 0 ]`：引数が1個以上残っている

### case文

値によって処理を分岐する。

```bash
case "$1" in
  --fix)
    # --fix の処理
    ;;
  -h | --help)
    # -h または --help の処理
    ;;
  *)
    # その他の処理
    ;;
esac
```

- `)`：パターンの終わり
- `|`：または
- ：その他すべて
- `;;`：その分岐の処理の終わり
- `esac`：case文の終わり

### shift

現在の引数を1つずらす。

```bash
shift
```

例えば、

```
$1 = --fix
$2 = --help
```

で `shift` すると、

```
$1 = --help
```

になる。

### while + case + shift

コマンドライン引数を1つずつ処理するときによく組み合わせる。

```bash
while [ $# -gt 0 ]; do
  case "$1" in
    --fix)
      FIX_MODE=true
      ;;
    -h | --help)
      usage
      exit 0
      ;;
    *)
      usage
      exit 1
      ;;
  esac

  shift
done
```

処理の流れ：

```
引数が残っている？
    ↓ Yes
$1をcaseで判定
    ↓
処理
    ↓
shiftで先頭の引数を取り除く
    ↓
次の引数へ
    ↓
引数がなくなるまで繰り返す
```

___


## Stimulusのアクションパラメータ

`data-xxx-yyy-param` で値を渡すと、`event.params.yyy` で取得できる。

```html
<button
  data-action="image-modal#open"
  data-image-modal-url-param="image.jpg"
  data-image-modal-caption-param="表ラベル">
</button>
```

```jsx
open(event) {
  const { url, caption } = event.params
}
```

### 対応関係

`data-<controller>-<param>-param`

→ `event.params.<param>`

例：

`data-image-modal-url-param`

→ `event.params.url`

`data-image-modal-caption-param`

→ `event.params.caption`

___
## `<dialog>` の主なメソッド・プロパティ

`<dialog>` 要素はJavaScriptから開閉や状態確認ができる。

### メソッド

```jsx
dialog.show()        // ダイアログを開く
dialog.showModal()   // モーダルとして開く
dialog.close()       // ダイアログを閉じる
```

### プロパティ

```jsx
dialog.open          // 開いているかどうか（true / false）
```

### Stimulusと組み合わせると

```jsx
this.dialogTarget.showModal()
```

- `this.dialogTarget` → StimulusのTarget
- `.showModal()` → `<dialog>` の標準メソッド

```jsx
this.dialogTarget.open
```

- `this.dialogTarget` → StimulusのTarget
- `.open` → `<dialog>` の標準プロパティ

___


## Stimulusの `has○○Target`

`static targets` で定義したTargetが存在するかを確認するためのプロパティ。

```jsx
static targets = ["dialog"]

this.hasDialogTarget
// → Targetが存在すれば true
// → 存在しなければ false
```

### 命名規則

`has○○Target`

- `dialog` → `hasDialogTarget`
- `image` → `hasImageTarget`
- `caption` → `hasCaptionTarget`

### 今回の用途

Targetが存在しない可能性がある場合に、安全に処理するために使う。

```jsx
if (this.hasDialogTarget) {
  this.dialogTarget.close()
}
```

※ `has○○Target` は、**Stimulusが提供するプロパティ**。

___

## `<figure>`要素

- **画像・図などの独立したコンテンツと、その説明をひとまとまりにするHTML要素**
- 説明には`<figcaption>`を使う
- 「画像＋キャプション」のような構造に適している
- `<div>`でも表示できるが、`<figure>`の方が意味（セマンティクス）を明確に表現できる

### 例

```html
<figure>
  <img src="sake.jpg" alt="日本酒のラベル">
  <figcaption>表ラベル</figcaption>
</figure>
```

___

## Turbo キャッシュと `<dialog>`

### 問題

拡大モーダルを開いたままページを離れると、**開いた状態が Turbo のキャッシュに残る**。
`disconnect()` で閉じていたが効いていなかった。

### 理由：Turbo の実行順

```
turbo:before-cache → スナップショット取得 → DOM 差し替え（ここで disconnect）
```

`disconnect()` はキャッシュを取った**後**に呼ばれるので間に合わない。

### 実測（Chrome）

```jsx
document.addEventListener("turbo:before-cache", () => {
  console.log(document.querySelector("dialog.modal")?.hasAttribute("open"))
})
```

- 離脱時: `true` → キャッシュには開いた状態が入っていた
- 復元後の DOM: `false` → Turbo が取り直すため、画面上のバグは出なかった

### 対応

```
<div data-controller="image-modal"
     data-action="turbo:before-cache@document->image-modal#close">
```

```jsx
close() {
  if (this.hasDialogTarget && this.dialogTarget.open) this.dialogTarget.close()
}
```

### 学び

- Stimulus は `@document` でグローバルイベントを拾える。
- DaisyUI の modal は `[open]` 属性だけで全画面表示になる（`showModal()` 不要）

# **8/19(水)**
## 【学習時間】
2.6h

## 【学習内容】
- 卒制
___
## 卒制概要
日本酒の記録アプリ。
## 【やったこと】
- MVPリリース
- 一覧画面の修正

## 【今後の予定】
- 日本酒詳細画面の作成
___
## Turbo Frame内のリンクをトップレベル遷移にする

Turbo Frame 内にあるリンクは、デフォルトでは**そのFrame内で遷移**する。

```
<%= link_to sake_log_path(sake_log) %>
```

そのため、2ページ目以降のカードなど、特定のFrame内にあるリンクから詳細画面へ遷移すると、RailsにはTurbo Frameリクエストとして送られる。

```ruby
turbo_frame_request?
# => true
```

### ページ全体を遷移させたい場合

リンクに以下を指定する。

```
data: { turbo_frame: "_top" }
```

```
<%= link_to sake_log_path(sake_log),
      data: { turbo_frame: "_top" } %>
```

これにより、

- 親のTurbo Frame内ではなく**ページ全体を遷移**
- `turbo_frame_request?` は基本的に `false`
- 通常の詳細画面などを返せる

という動作になる。

### 今回のケース

2ページ目以降は、

```
sake_logs_page_2 Frame
└── カード
    └── 詳細リンク
```

となっている。

Frame内で詳細リンクをクリックすると、`show` アクションは「2ページ目のFrame ID」を知らないため、Frame用のレスポンスを返せず **Content missing** になる可能性がある。

そのため、**詳細リンクと編集リンクには `data: { turbo_frame: "_top" }` を付ける**。

一方、削除リンクはTurbo Streamでカードだけを削除する設計なので、`_top` は付けない。

> **覚え方：**
> 
> - Frame内で処理 → デフォルト
> - ページ全体を遷移 → `turbo_frame: "_top"`
> - Frame内の一部分だけ更新・削除 → Turbo Streamが適する

___
## Railsの名前付きルート（`as:`）

- `as: :mypage` は、**ルートに名前を付ける**ための指定。
- `get "mypage", to: "users#show", as: :mypage` とすると、`mypage_path` というURLヘルパーが生成される。
- `mypage_path` を使うことで、ビューから `/mypage` へのリンクを簡潔に書ける。

```ruby
get "mypage", to: "users#show", as: :mypage
```

```
<%= link_to "マイページ", mypage_path %>
```

`mypage_path` は `/mypage` を返す。

**ポイント：**

`as:` は、URLそのものを変更する指定ではなく、**RailsでURLを参照するための名前（URLヘルパー）を付ける指定**。



