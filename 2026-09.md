# **9/7(月)**
## 【学習時間】
2.3h(累計:1,603h/1,000h)  

## 【学習内容】
- アプリ開発
___
## 卒制概要
日本酒の記録アプリ。
## 【やったこと】
AIによる画像の読み込み機能実装

## 【今後の予定】
AIによる画像の読み込み機能実装
X投稿機能実装
___

## Stimulusの`○○ValueChanged()`

`static values`で宣言した値には、値の初期読み込み時や変更時に呼ばれるコールバックを定義できる。

```jsx
static values = {
  remaining: Number
}

remainingValueChanged(currentValue, previousValue) {
  this.remainingTarget.textContent = `本日あと${currentValue}回`
}
```

命名規則は次のとおり。

```
値の名前 + ValueChanged
```

`remaining`なら`remainingValueChanged()`、`url`なら`urlValueChanged()`となる。

主な実行タイミング：

- Stimulusが初期値を読み込んだとき
- `this.remainingValue = 10`のように値を変更したとき
- 対応するHTMLの`data-*-remaining-value`属性が変更されたとき

引数では、変更後と変更前の値を受け取れる。

```jsx
remainingValueChanged(currentValue, previousValue) {
  // currentValue：変更後の値
  // previousValue：変更前の値
}
```

対応する値を`static values`に宣言することが前提。

```jsx
static values = {
  remaining: Number
}
```

___

## `this.element.closest("form")`

Stimulusの`this.element`は、`data-controller`が付いているHTML要素を表す。

```jsx
this.element.closest("form")
```

`closest()`は、`this.element`自身から親方向へさかのぼり、最初に一致した要素を取得するDOMメソッドである。

今回の場合は、`label-extraction`コントローラーを囲む最も近い`form`要素を取得している。

```jsx
const form = this.element.closest("form")
```

取得したフォームに対して`querySelector()`を使えば、コントローラーの外側にある同じフォーム内の要素も検索できる。

```jsx
const input = this.element
  .closest("form")
  .querySelector('input[type="file"]')
```

一致する要素が存在しない場合、`closest()`は`null`を返す。

```
this        → Stimulusコントローラー
this.element → data-controllerが付いたHTML要素
closest()   → 自身から親方向へ一致する要素を探す
```

___

## `FormData`

`FormData`は、文字列や画像ファイルなどをフォーム送信形式でまとめるためのブラウザのWeb API。

```jsx
const formData = new FormData()
```

これで空の`FormData`を作成し、`append()`でデータを追加する。

```jsx
formData.append("name", "吟ログ")
formData.append("front_label_image", file, "front_label.jpg")
```

基本形：

```jsx
formData.append(項目名, 値)
formData.append(項目名, ファイル, ファイル名)
```

作成したデータは、`fetch()`の`body`に指定して送信できる。

```jsx
fetch("/api/label_extraction", {
  method: "POST",
  body: formData
})
```

Railsでは、項目名を使って受け取る。

```ruby
params[:name]
params[:front_label_image]
```

`FormData`を送信するときは、ブラウザが適切な区切り情報を付けるため、通常は`Content-Type: multipart/form-data`を手動で設定しない。

```
FormData
  → ブラウザが提供するWeb API
  → 文字列やファイルを送信用にまとめる
  → append()でデータを追加する
  → fetch()のbodyに指定して送信する
```

___

## Promiseと`await`

### Promise

`Promise`は変数の種類ではなく、「非同期処理の将来の結果」を表すJavaScriptのオブジェクト。

Promiseには3つの状態がある。

- `pending`：処理中
- `fulfilled`：処理成功
- `rejected`：処理失敗

`async`を付けた関数は、必ずPromiseを返す。

```jsx
async function getNumber() {
  return 10
}

const result = getNumber()
// => Promise
```

### `await`

`await`は、Promiseの処理が完了するのを待ち、成功後の実際の値を取り出す。

```jsx
const result = await getNumber()
// => 10
```

通常、`await`は`async`関数の中で使用する。

```jsx
async function execute() {
  const result = await getNumber()
}
```

Promiseが失敗すると`await`部分でエラーが発生するため、必要に応じて`try...catch`で処理する。

```jsx
try {
  const result = await getNumber()
} catch (error) {
  console.error(error)
}
```

```
async   → 関数がPromiseを返す
Promise → 非同期処理の将来の結果を表す
await   → Promiseの完了を待ち、成功結果を取り出す
```

`await`はブラウザ全体ではなく、その`async`関数の後続処理だけを待機させる。

___

### JavaScriptのスプレッド構文（`...`）

`...`は、配列の要素をその場所へ1つずつ展開する構文。

```jsx
const names = ["A", "B"]

const result = ["先頭", ...names]
// => ["先頭", "A", "B"]
```

`...`を付けない場合は、配列が入れ子になる。

```jsx
const result = ["先頭", names]
// => ["先頭", ["A", "B"]]
```

Rubyでは、配列内の`*`に相当する。

```ruby
result = ["先頭", *names]
# => ["先頭", "A", "B"]
```

Rubyの`flat_map`とは異なり、要素を変換する処理はなく、単純に配列を展開する。

___

## `module_function`

モジュール内に定義したメソッドを、モジュール自身のメソッドとして呼び出せるようにする。

```ruby
module Calculator
  module_function

  def add(a, b)
    a + b
  end
end

Calculator.add(1, 2)
# => 3
```

インスタンスを作る必要がなく、状態を持たない共通処理をまとめる場合に便利。

```ruby
module_function
```

より後に定義したメソッドが対象になる。

今回の例では、次のように呼び出せる。

```ruby
LabelExtractionEval.read_image(path)
LabelExtractionEval.match?(expected, actuals)
LabelExtractionEval.judge(row, result)
```

`def self.add`のように、各メソッドへ`self.`を付けて定義する方法でも同様に呼び出せる。

___

## `Array#intersect?`

2つの配列に、共通する要素が1つでもあるかを判定する。戻り値は`true`または`false`。

```ruby
["赤武", "AKABU"].intersect?(["AKABU", "獺祭"])
# => true

["赤武", "AKABU"].intersect?(["獺祭"])
# => false
```

今回のコードでは、

```ruby
expected.split("/").intersect?(actuals.compact)
```

次の順に処理している。

1. 正解を`/`で分割して配列にする
2. 実際の候補から`nil`を除く
3. 両方の配列に共通する値があるか判定する

```ruby
expected = "赤武/AKABU"
actuals = ["AKABU", nil]

expected.split("/")
# => ["赤武", "AKABU"]

actuals.compact
# => ["AKABU"]

["赤武", "AKABU"].intersect?(["AKABU"])
# => true
```

比較は文字列の完全一致であり、部分一致ではない。

```ruby
["赤武"].intersect?(["赤武 純米吟醸"])
# => false
```

___

## CSVライブラリ

RubyでCSVファイルを扱うための標準ライブラリ。

### 使い方

```ruby
require "csv"
```

でライブラリを読み込む。読み込むことで、`CSV`クラスを利用できるようになる。

### 使えるメソッド例

以下のCSVファイルがある場合。

```
brand_name,product_name
赤武,純米吟醸
獺祭,磨き三割九分
```

```ruby
CSV.foreach("sample.csv", headers: true) do |row|
  puts row["brand_name"]
  puts row["product_name"]
end
```

出力結果：

```
赤武
純米吟醸
獺祭
磨き三割九分
```

`CSV.foreach`は、CSVファイルを1行ずつ順番に読み込むメソッド。

`headers: true`を指定すると、先頭行を列名として扱い、`row["brand_name"]`のように列名で値を取得できる。

___

## `abort`

エラーメッセージを表示し、Rubyプログラムをその場で異常終了させるRuby標準のメソッド。

```ruby
abort("エラーメッセージ")
```

必須の値やファイルがなく、後続処理を続けられない場合などに使用する。

```ruby
abort("EVAL_DIRを指定してください") if eval_dir.blank?
```

この例では、`eval_dir`が空の場合にメッセージを表示し、以降の処理を実行せず終了する。

`return`が現在のメソッドから抜けるのに対し、`abort`は実行中のプログラム全体を終了させる。

___

## `Hash.new`

新しい空のHashを作成するRubyのメソッド。引数を指定すると、存在しないキーを参照したときのデフォルト値になる。

```ruby
totals = Hash.new(0)

totals[:brand]
# => 0

totals[:brand] += 1
# => 1
```

値を参照しただけでは、キーはHashに登録されない。

```ruby
totals = Hash.new(0)
totals[:brand]
totals
# => {}
```

引数には文字列や`nil`なども指定できる。引数を省略した場合のデフォルト値は`nil`。

```ruby
Hash.new("未設定")
Hash.new(nil)
Hash.new # Hash.new(nil)と同じ
```

配列などの変更可能な値を直接指定すると、同じオブジェクトが全キーで共有されるため注意する。

```ruby
items = Hash.new([])

items[:brand] << "赤武"

items[:product]
# => ["赤武"]
```

`:brand`へ追加したはずなのに、`:product`でも同じ配列が返ってしまう。また、この時点では`:brand`というキー自体もHashに登録されていない。

キーごとに別の配列を使う場合は、ブロック形式にする。

```ruby
# キーごとに別の配列を作る
items = Hash.new { |hash, key| hash[key] = [] }

items[:brand] << "赤武"
items[:product] << "純米吟醸"

items
# => {
#   brand: ["赤武"],
#   product: ["純米吟醸"]
# }
```

### 主な使い分け例

```ruby
# 数値カウンター
totals = Hash.new(0)

# 未設定を表す
statuses = Hash.new("未設定")

# キーごとに別の配列を持たせる
items = Hash.new { |hash, key| hash[key] = [] }
```

___

## `File.join`

複数の文字列を、パスの区切り文字を使って結合するRubyのメソッド。

```ruby
File.join("/path/to/images", "label.jpg")
# => "/path/to/images/label.jpg"
```

複数の要素もまとめて結合できる。

```ruby
File.join("/path", "to", "images", "label.jpg")
# => "/path/to/images/label.jpg"
```

Javaの`Path.resolve()`に近い役割を持つ。

`File.join`はパスを表す文字列を作るだけで、ファイルの作成・読み込み・存在確認は行わない。